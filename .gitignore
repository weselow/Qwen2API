node_modules
package-lock.json
.env
data/data.json
data
caches
logs/
*.log
pkg_dist/*
pkg_dist/
.idea
/public/dist
.vscode

# Beads / Dolt files (added by bd init)
.beads/
.dolt/
*.db
.beads-credential-key
issues.jsonl

# Beads orchestration
.worktrees/

# Our fork notes (not for upstream)
docs_fork/

# Локальные конфиги ИИ-инструментов (содержат API-ключи) — не коммитить
.mcp.json
.codex/

# Локальный docker-стенд: данные, журналы, кэш картинок
.volumes/

# Локальные настройки Claude Code (разрешения конкретной машины)
.claude/settings.local.json
